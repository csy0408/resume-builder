# Phase 2: AI 集成 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 集成 Claude API，实现 AI 自动扫描项目、简历解析、JD 匹配、变体生成、写作助手五大 AI 能力。

**Architecture:** 统一 `src/services/claude.ts` 服务层封装所有 Claude API 调用。Settings 页面管理 API Key。各个 AI 功能页面通过 `claude.ts` 调用对应 prompt 函数，结果写入 Zustand store。所有调用纯前端（用户自带 Key）。

**Tech Stack:** @anthropic-ai/sdk (已安装), Claude Sonnet 4, React 19, Zustand 5

---

## 文件变更总览

```
新增：
  src/services/claude.ts              ← Claude API 统一服务层
  src/components/common/AiButton.tsx  ← AI 操作按钮（loading 状态）
  src/pages/JDInput.tsx              ← JD 输入 & 匹配页面
  src/pages/AiScan.tsx              ← AI 项目扫描页面
  src/components/experience/AiVariantGenerator.tsx
  src/components/experience/WritingAssistant.tsx

修改：
  src/pages/Settings.tsx             ← 添加 API Key 管理
  src/pages/Profile.tsx              ← 添加扫描/上传入口
  src/App.tsx                         ← 更新路由
  src/store/profileStore.ts          ← 添加 AI 相关 actions
  src/pages/Editor.tsx               ← 添加写作助手入口
```

---

### Task 1: Settings — API Key 管理

**Files:**
- Modify: `src/pages/Settings.tsx`

添加 API Key 输入区。使用 `localStorage` 存储（不存 Zustand，安全隔离）。

```typescript
// 在 Settings.tsx 现有 sections 之上添加:

import { useState, useEffect } from 'react';

// 在组件内:
const [apiKey, setApiKey] = useState('');
const [keySaved, setKeySaved] = useState(false);

useEffect(() => {
  const saved = localStorage.getItem('resume-builder-api-key');
  if (saved) { setApiKey(saved); setKeySaved(true); }
}, []);

const handleSaveKey = () => {
  if (!apiKey.trim()) return;
  localStorage.setItem('resume-builder-api-key', apiKey.trim());
  setKeySaved(true);
};

const handleClearKey = () => {
  localStorage.removeItem('resume-builder-api-key');
  setApiKey('');
  setKeySaved(false);
};
```

在 Settings 页面的"数据管理"section 之前添加：

```tsx
<section className="bg-white rounded-xl p-6 shadow-sm mb-6">
  <h3 className="text-lg font-bold text-[var(--color-primary-dark)] mb-4">Claude API Key</h3>
  <p className="text-xs text-gray-400 mb-3">
    用于 AI 功能（项目扫描、JD 匹配、简历解析）。Key 仅保存在浏览器本地，不会上传。
  </p>
  <div className="flex gap-3">
    <input
      type="password"
      value={apiKey}
      onChange={(e) => { setApiKey(e.target.value); setKeySaved(false); }}
      placeholder="sk-ant-api03-..."
      className="flex-1 px-3 py-2 border border-gray-200 rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-[var(--color-primary)]"
    />
    <button onClick={handleSaveKey} className="px-4 py-2 bg-[var(--color-primary)] text-white text-sm rounded-lg">
      保存
    </button>
    {keySaved && (
      <button onClick={handleClearKey} className="px-4 py-2 border border-red-200 text-red-500 text-sm rounded-lg">
        清除
      </button>
    )}
  </div>
  {keySaved && <p className="text-xs text-green-500 mt-2">API Key 已保存</p>}
</section>
```

验证 `npx tsc --noEmit`，提交。

---

### Task 2: Claude API 服务层

**Files:**
- Create: `src/services/claude.ts`

```typescript
import Anthropic from '@anthropic-ai/sdk';

function getClient(): Anthropic | null {
  const key = localStorage.getItem('resume-builder-api-key');
  if (!key) return null;
  return new Anthropic({ apiKey: key, dangerouslyAllowBrowser: true });
}

async function chat(system: string, user: string, maxTokens = 4096): Promise<string> {
  const client = getClient();
  if (!client) throw new Error('API Key 未设置');
  const msg = await client.messages.create({
    model: 'claude-sonnet-4-20250514',
    max_tokens: maxTokens,
    system,
    messages: [{ role: 'user', content: user }],
  });
  const text = msg.content[0]?.type === 'text' ? msg.content[0].text : '';
  return text;
}

// ---- Project Scanner ----
interface ScanResult { projectName: string; role: string; techStack: string[]; baseDescription: string; variants: { label: string; description: string; keywords: string[] }[] }

export async function scanProject(files: { name: string; content: string }[]): Promise<ScanResult[]> {
  const fileList = files.map(f => `### ${f.name}\n\`\`\`\n${f.content.slice(0, 3000)}\n\`\`\``).join('\n\n');
  const text = await chat(
    '你是技术简历专家。从项目文件中提取开发经历，返回 JSON 数组。每个经历包含 projectName, role, techStack, baseDescription, variants（不同岗位侧重点的变体，如前端/后端/算法）。只返回 JSON，不要其他文字。',
    `分析以下项目文件，提取可作为简历素材的开发经历:\n\n${fileList}`
  );
  return JSON.parse(extractJson(text));
}

// ---- Resume Parser ----
export async function parseResume(text: string): Promise<{
  basic: { name: string; phone: string; email: string; location: string; github?: string };
  education: { school: string; major: string; degree: string; startDate: string; endDate?: string }[];
  experiences: { projectName: string; role: string; startDate: string; endDate?: string; techStack: string[]; baseDescription: string }[];
  skills: { category: string; name: string }[];
}> {
  const result = await chat(
    '你是简历解析专家。从简历文本中提取结构化信息。返回 JSON，包含 basic（name,phone,email,location,github）、education（school,major,degree,startDate,endDate）、experiences（projectName,role,startDate,endDate,techStack,baseDescription）、skills（category,name）。只返回 JSON。',
    `解析以下简历:\n\n${text}`
  );
  return JSON.parse(extractJson(result));
}

// ---- JD Parser ----
export async function parseJD(jdText: string): Promise<{
  title: string; company?: string; techStack: string[];
  requirements: { category: 'skill'|'experience'|'education'|'soft-skill'; keyword: string; importance: 'required'|'preferred'; yearsRequired?: number }[];
  summary: string;
}> {
  const result = await chat(
    '你是招聘需求分析专家。从岗位JD中提取结构化信息。返回 JSON，包含 title,company,techStack,requirements（数组，每项有 category/keyword/importance/yearsRequired）,summary。只返回 JSON。',
    `分析这个岗位JD:\n\n${jdText}`
  );
  return JSON.parse(extractJson(result));
}

// ---- JD Matcher ----
export async function matchJD(profile: object, jdAnalysis: object): Promise<{
  selectedExperiences: { experienceId: string; variantId?: string; relevance: number }[];
  gaps: { keyword: string; suggestion: string }[];
  score: number;
  suggestions: string[];
}> {
  const result = await chat(
    '你是简历匹配专家。对比用户 Profile 和岗位 JD，选择最相关的经历和变体，给出匹配度评分和差距分析。返回 JSON：selectedExperiences(experienceId,variantId,relevance)、gaps(keyword,suggestion)、score(0-100)、suggestions。只返回 JSON。',
    `用户 Profile:\n${JSON.stringify(profile, null, 2)}\n\nJD 分析:\n${JSON.stringify(jdAnalysis, null, 2)}`
  );
  return JSON.parse(extractJson(result));
}

// ---- Variant Generator ----
export async function generateVariants(experience: object, targetRoles: string[]): Promise<{ label: string; description: string; keywords: string[] }[]> {
  const result = await chat(
    '你是技术简历写作专家。为同一段经历生成不同岗位侧重点的描述变体。返回 JSON 数组，每项包含 label, description, keywords。',
    `经历:\n${JSON.stringify(experience, null, 2)}\n\n目标岗位方向:\n${targetRoles.join(', ')}\n\n为每个方向生成对应的经历描述变体。只返回 JSON 数组。`
  );
  return JSON.parse(extractJson(result));
}

// ---- Writing Assistant ----
export async function polishText(text: string, mode: 'polish' | 'star' | 'shorter' | 'longer' | 'technical' | 'management'): Promise<string> {
  const modePrompts: Record<string, string> = {
    polish: '润色以下简历描述，使其更专业、更有影响力，保持原意',
    star: '用 STAR 法则（Situation-Task-Action-Result）重写以下描述，突出量化成果',
    shorter: '将以下描述精简到 2-3 句话，保留核心信息',
    longer: '将以下描述扩展，补充更多细节和量化成果',
    technical: '将以下描述调整为技术导向，强调技术选型和架构决策',
    management: '将以下描述调整为管理导向，强调团队领导和项目协调能力',
  };
  return chat(modePrompts[mode] || modePrompts.polish, text, 1024);
}

// ---- Helper ----
function extractJson(text: string): string {
  const match = text.match(/\{[\s\S]*\}|\[[\s\S]*\]/);
  return match ? match[0] : text;
}
```

验证 `npx tsc --noEmit`，提交。

---

### Task 3: AI 项目扫描页面

**Files:**
- Create: `src/pages/AiScan.tsx`
- Modify: `src/pages/Profile.tsx`, `src/App.tsx`

创建 `src/pages/AiScan.tsx`：

文件选择器（多个文件）+ 项目名称 + 预览选择的文件列表 → 点击"开始扫描" → 调用 `scanProject()` → 展示结果 → 用户勾选要导入的经历 → 点击"导入" → 写入 store。

关键代码结构：
```tsx
// 文件读取
const handleFiles = async (e: React.ChangeEvent<HTMLInputElement>) => {
  const files = Array.from(e.target.files || []);
  const loaded: { name: string; content: string }[] = [];
  for (const f of files) {
    const content = await f.text();
    loaded.push({ name: f.name, content });
  }
  setFileList(loaded);
};

// AI 扫描
const handleScan = async () => {
  setScanning(true);
  try {
    const results = await scanProject(fileList);
    setScanResults(results);
  } catch (e) { setError((e as Error).message); }
  setScanning(false);
};

// 导入到 store
const handleImport = () => {
  checkedResults.forEach(r => addExperience({ ...r, id: '', source: 'ai-scan', verified: true, variants: r.variants || [] }));
  navigate('/profile');
};
```

**Profile 页面集成**：在"添加经历"按钮旁边加"AI 扫描"链接。

**路由**：在 App.tsx 添加 `/profile/scan` 路由。

---

### Task 4: 上传简历解析

**Files:**
- Modify: `src/pages/Profile.tsx`, `src/pages/AiScan.tsx`

在 Profile 页面或 AiScan 页面添加"上传简历"功能：
- 文件上传（PDF/Word .docx）
- 需要先将 PDF/Word 转为文本。推荐使用 `pdfjs-dist` 读取 PDF，`mammoth` 读取 Word
- 调用 `parseResume(text)` → 自动填充 Profile

```bash
npm install pdfjs-dist mammoth
```

在 AiScan 页面添加 Tab 或按钮切换"项目扫描"和"上传简历"两种模式。简历解析后直接 `importProfile(parsedData)`。

---

### Task 5: JD 输入 & 匹配页面

**Files:**
- Create: `src/pages/JDInput.tsx`
- Modify: `src/App.tsx`

创建完整的 JD 匹配流程页面：

1. JD 输入区（textarea）+ "解析 JD" 按钮 → 调用 `parseJD()`
2. 展示解析结果（岗位名、技术栈、要求列表）
3. "开始匹配"按钮 → 调用 `matchJD(profile, jdAnalysis)` 
4. 展示匹配结果：
   - 匹配度评分（圆形进度条）
   - 选中的经历 + 推荐变体
   - 差距分析（缺失技能 + 建议）
5. "生成简历"按钮 → 跳转到 Editor 页面并预选匹配的经历和变体

```tsx
// JDInput.tsx 核心结构
const [jdText, setJdText] = useState('');
const [jdResult, setJdResult] = useState<JdAnalysis | null>(null);
const [matchResult, setMatchResult] = useState<MatchResult | null>(null);
const [loading, setLoading] = useState('');

const handleParse = async () => {
  setLoading('parse');
  const r = await parseJD(jdText);
  setJdResult(r);
  setLoading('');
};

const handleMatch = async () => {
  setLoading('match');
  const r = await matchJD(profile, jdResult);
  setMatchResult(r);
  setLoading('');
};
```

**路由**：将 `/jd` 从占位改为 `<JDInput />`。

---

### Task 6: AI 变体生成器

**Files:**
- Create: `src/components/experience/AiVariantGenerator.tsx`
- Modify: `src/components/experience/VariantEditor.tsx`

在 VariantEditor 组件中添加"AI 生成变体"按钮：

```tsx
// AiVariantGenerator.tsx — 弹窗组件
// 用户输入目标岗位方向（如"前端开发 后端开发 算法工程师"）
// → 调用 generateVariants(experience, roles)
// → 展示生成的变体 → 选择导入

const handleGenerate = async () => {
  setLoading(true);
  const roles = roleInput.split(/[,，\s]+/).filter(Boolean);
  const variants = await generateVariants(exp, roles);
  setGeneratedVariants(variants);
  setLoading(false);
};
```

在 VariantEditor 的 `showForm` 旁边添加 `<AiVariantGenerator experience={experience} />`。

---

### Task 7: AI 写作助手

**Files:**
- Create: `src/components/experience/WritingAssistant.tsx`
- Modify: `src/pages/Editor.tsx`

弹窗组件，选中文本后点击"AI 润色"：

```tsx
// WritingAssistant.tsx
const MODES = [
  { key: 'polish', label: '润色优化' },
  { key: 'star', label: 'STAR 法则' },
  { key: 'shorter', label: '精简' },
  { key: 'technical', label: '技术导向' },
  { key: 'management', label: '管理导向' },
] as const;

const handlePolish = async (mode: string) => {
  setLoading(true);
  const result = await polishText(inputText, mode);
  setOutput(result);
  setLoading(false);
};
```

在 Editor 页面编辑描述时添加"AI 润色"按钮，或在 ExperienceForm 的 textarea 旁边添加。

---

## 完成检查清单

- [ ] `npx tsc --noEmit` — 0 errors
- [ ] `npm run build` — 构建成功
- [ ] Settings 页面可输入/保存/清除 API Key
- [ ] AI 项目扫描：选文件 → 扫描 → 导入经历
- [ ] 上传简历解析：上传 PDF → AI 提取 → 填充 Profile
- [ ] JD 解析 & 匹配：粘贴 JD → 解析 → 匹配 → 展示结果
- [ ] 变体生成：输入方向 → AI 生成变体 → 导入
- [ ] 写作助手：选文本 → 选模式 → AI 润色
