# Phase 0: 项目初始化 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 从零搭建简历制作平台的完整开发环境：React + Vite + TypeScript 脚手架、依赖安装、工具链配置、基础目录结构、Git 仓库初始化、GitHub Actions 自动部署。

**Architecture:** 使用 `npm create vite@latest` 创建项目骨架，手动安装项目所需依赖，配置 Tailwind CSS + ESLint + Prettier。创建符合技术规范的空目录结构和 TypeScript 类型定义骨架。GitHub Actions 配置自动构建部署到 GitHub Pages。

**Tech Stack:** Node.js, npm, Vite 5, React 18, TypeScript 5, Tailwind CSS 3, Git, GitHub Actions

**前提条件:** Node.js 18+ 已安装，Git 已安装，有 GitHub 账号

---

## 文件结构（Phase 0 产出）

```
简历制作/
├── src/
│   ├── components/
│   │   ├── common/          ← 空，Phase 1 开始填充
│   │   ├── experience/
│   │   ├── resume/
│   │   └── layout/
│   ├── pages/
│   │   └── Home.tsx         ← 占位首页
│   ├── hooks/               ← 空
│   ├── services/            ← 空
│   ├── store/               ← 空
│   ├── templates/           ← 空
│   ├── types/
│   │   ├── profile.ts       ← 类型骨架
│   │   ├── resume.ts
│   │   └── jd.ts
│   ├── utils/
│   │   └── storage.ts       ← localStorage 工具
│   ├── App.tsx              ← 路由壳子
│   ├── main.tsx
│   └── index.css            ← Tailwind 入口
├── .github/workflows/
│   └── deploy.yml           ← GitHub Pages 自动部署
├── docs/                    ← 已存在
├── devlog/                  ← 已存在
├── package.json
├── tsconfig.json
├── vite.config.ts
├── tailwind.config.js
├── postcss.config.js
├── .eslintrc.cjs
├── .prettierrc
├── .gitignore
├── index.html
└── CLAUDE.md                ← 已存在
```

---

### Task 1: 创建 Vite + React + TypeScript 项目骨架

**Files:**
- Create: `package.json`, `tsconfig.json`, `vite.config.ts`, `index.html`, `src/main.tsx`, `src/App.tsx`, `src/index.css`, `.gitignore`

- [ ] **Step 1: 使用 Vite 脚手架创建项目**

```bash
cd D:/Claude/projects
npm create vite@latest 简历制作 -- --template react-ts
```

选择 react-ts 模板。如果提示目录已存在，先用 `rm -rf` 清理 src/ 等非 docs/devlog 目录，然后重新创建。

- [ ] **Step 2: 验证项目可运行**

```bash
cd D:/Claude/projects/简历制作
npm install
npm run dev
```

预期: 开发服务器在 `http://localhost:5173` 启动，显示默认 Vite + React 页面。

Ctrl+C 停止服务器。

- [ ] **Step 3: 提交**

```bash
cd D:/Claude/projects/简历制作
git init
git add package.json tsconfig.json tsconfig.node.json vite.config.ts index.html .gitignore src/ public/
git commit -m "chore: scaffold Vite + React + TypeScript project"
```

---

### Task 2: 安装项目依赖

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 安装运行时依赖**

```bash
cd D:/Claude/projects/简历制作
npm install react-router-dom zustand @anthropic-ai/sdk jspdf html2canvas docx @tiptap/react @tiptap/starter-kit @tiptap/extension-placeholder
```

包说明：
- `react-router-dom` — 路由
- `zustand` — 状态管理
- `@anthropic-ai/sdk` — Claude API
- `jspdf` + `html2canvas` — PDF 导出
- `docx` — Word 导出
- `@tiptap/react` + `@tiptap/starter-kit` + `@tiptap/extension-placeholder` — 富文本编辑器

- [ ] **Step 2: 安装开发依赖**

```bash
npm install -D tailwindcss @tailwindcss/vite postcss autoprefixer
npm install -D eslint prettier eslint-config-prettier eslint-plugin-react-hooks @typescript-eslint/eslint-plugin @typescript-eslint/parser
npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom
```

- [ ] **Step 3: 验证安装**

```bash
npm ls react-router-dom zustand  # 确认核心包存在
```

- [ ] **Step 4: 提交**

```bash
git add package.json package-lock.json
git commit -m "chore: install project dependencies"
```

---

### Task 3: 配置 Tailwind CSS

**Files:**
- Create: `postcss.config.js`
- Modify: `vite.config.ts`, `src/index.css`

- [ ] **Step 1: 创建 PostCSS 配置**

创建 `postcss.config.js`:
```js
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}
```

- [ ] **Step 2: 在 Vite 配置中引入 Tailwind**

读取当前 `vite.config.ts`，修改为：
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [
    react(),
    tailwindcss(),
  ],
  base: '/resume-builder/',  // GitHub Pages 子路径，按实际仓库名调整
})
```

- [ ] **Step 3: 设置 Tailwind 入口**

将 `src/index.css` 内容替换为：
```css
@import "tailwindcss";

/* 自定义主题变量 — 后续 Phase 扩展 */
:root {
  --color-primary: #2E86AB;
  --color-primary-dark: #1B3A5C;
  --color-bg: #F5F7FA;
}
```

- [ ] **Step 4: 验证 Tailwind 生效**

在 `src/App.tsx` 中临时加入：
```tsx
<h1 className="text-3xl text-[#2E86AB]">Resume Builder</h1>
```

运行 `npm run dev` 确认文字显示为淡蓝色且字号正确。Ctrl+C 停止。

- [ ] **Step 5: 提交**

```bash
git add postcss.config.js vite.config.ts src/index.css src/App.tsx
git commit -m "chore: configure Tailwind CSS with custom theme"
```

---

### Task 4: 配置 ESLint + Prettier

**Files:**
- Create: `.prettierrc`, `.eslintrc.cjs`
- Modify: `package.json`

- [ ] **Step 1: 创建 Prettier 配置**

创建 `.prettierrc`:
```json
{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "all",
  "printWidth": 100
}
```

- [ ] **Step 2: 创建 ESLint 配置**

创建 `.eslintrc.cjs`:
```javascript
module.exports = {
  root: true,
  env: { browser: true, es2020: true },
  extends: [
    'eslint:recommended',
    'plugin:@typescript-eslint/recommended',
    'plugin:react-hooks/recommended',
    'prettier',
  ],
  ignorePatterns: ['dist', '.eslintrc.cjs'],
  parser: '@typescript-eslint/parser',
  plugins: ['react-hooks', '@typescript-eslint'],
  rules: {
    'react-hooks/rules-of-hooks': 'error',
    'react-hooks/exhaustive-deps': 'warn',
    '@typescript-eslint/no-unused-vars': ['warn', { argsIgnorePattern: '^_' }],
  },
}
```

- [ ] **Step 3: 添加 lint 脚本**

在 `package.json` 的 `"scripts"` 中添加：
```json
"lint": "eslint src --ext .ts,.tsx",
"format": "prettier --write \"src/**/*.{ts,tsx,css,json}\""
```

- [ ] **Step 4: 验证 lint 可用**

```bash
npm run lint
```

预期: 0 errors, 最多几个 warnings。

- [ ] **Step 5: 提交**

```bash
git add .prettierrc .eslintrc.cjs package.json
git commit -m "chore: configure ESLint and Prettier"
```

---

### Task 5: 创建目录结构和类型定义

**Files:**
- Create: 空目录结构 + `src/types/profile.ts`, `src/types/resume.ts`, `src/types/jd.ts`, `src/utils/storage.ts`

- [ ] **Step 1: 创建目录结构**

```bash
cd D:/Claude/projects/简历制作/src
mkdir -p components/common components/experience components/resume components/layout
mkdir -p pages hooks services store templates/classic templates/modern templates/hybrid
mkdir -p types utils
```

- [ ] **Step 2: 创建类型定义 — profile.ts**

创建 `src/types/profile.ts`:
```typescript
export interface BasicInfo {
  name: string;
  photo?: string;
  phone: string;
  email: string;
  location: string;
  github?: string;
  website?: string;
}

export interface Education {
  id: string;
  school: string;
  major: string;
  degree: string;
  startDate: string;
  endDate?: string;
  gpa?: string;
}

export interface ExperienceVariant {
  id: string;
  label: string;
  description: string;
  keywords: string[];
  language: 'zh' | 'en';
}

export interface Experience {
  id: string;
  projectName: string;
  role: string;
  startDate: string;
  endDate?: string;
  techStack: string[];
  baseDescription: string;
  variants: ExperienceVariant[];
  source: 'ai-scan' | 'resume-parse' | 'manual';
  sourcePath?: string;
  verified: boolean;
  verifiedAt?: string;
}

export interface Skill {
  id: string;
  category: string;
  name: string;
  level?: string;
}

export interface UserProfile {
  basic: BasicInfo;
  education: Education[];
  experiences: Experience[];
  skills: Skill[];
  updatedAt: string;
}

export const EMPTY_PROFILE: UserProfile = {
  basic: {
    name: '',
    phone: '',
    email: '',
    location: '',
  },
  education: [],
  experiences: [],
  skills: [],
  updatedAt: new Date().toISOString(),
};
```

- [ ] **Step 3: 创建类型定义 — resume.ts**

创建 `src/types/resume.ts`:
```typescript
export interface ResumeContent {
  basic: {
    name: string;
    photo?: string;
    phone: string;
    email: string;
    location: string;
    github?: string;
    website?: string;
  };
  education: Array<{
    school: string;
    major: string;
    degree: string;
    date: string;
  }>;
  experiences: Array<{
    projectName: string;
    role: string;
    date: string;
    description: string;
    techStack: string[];
  }>;
  skills: string[];
}

export interface ResumeVersion {
  id: string;
  jobTitle: string;
  jdText: string;
  templateId: string;
  matchScore: number;
  selectedExperiences: string[];
  selectedVariants: Record<string, string>;
  content: ResumeContent;
  language: 'zh' | 'en';
  createdAt: string;
}

export interface TemplateConfig {
  id: string;
  name: string;
  description: string;
  showPhoto: boolean;
  colors: {
    primary: string;
    text: string;
    background: string;
    accent: string;
  };
}
```

- [ ] **Step 4: 创建类型定义 — jd.ts**

创建 `src/types/jd.ts`:
```typescript
export interface JdRequirement {
  category: 'skill' | 'experience' | 'education' | 'soft-skill';
  keyword: string;
  importance: 'required' | 'preferred';
  yearsRequired?: number;
}

export interface JdAnalysis {
  title: string;
  company?: string;
  techStack: string[];
  requirements: JdRequirement[];
  summary: string;
  rawText: string;
}

export interface MatchGap {
  requirement: JdRequirement;
  matched: boolean;
  suggestion?: string;
}

export interface MatchResult {
  score: number;
  matchedExperiences: Array<{
    experienceId: string;
    variantId: string;
    relevance: number;
  }>;
  gaps: MatchGap[];
  suggestions: string[];
}
```

- [ ] **Step 5: 创建 localStorage 工具**

创建 `src/utils/storage.ts`:
```typescript
const STORAGE_PREFIX = 'resume-builder-';

export function loadFromStorage<T>(key: string, fallback: T): T {
  try {
    const raw = localStorage.getItem(STORAGE_PREFIX + key);
    return raw ? (JSON.parse(raw) as T) : fallback;
  } catch {
    return fallback;
  }
}

export function saveToStorage<T>(key: string, data: T): void {
  localStorage.setItem(STORAGE_PREFIX + key, JSON.stringify(data));
}

export function removeFromStorage(key: string): void {
  localStorage.removeItem(STORAGE_PREFIX + key);
}

export function exportAsJson<T>(data: T, filename: string): void {
  const blob = new Blob([JSON.stringify(data, null, 2)], { type: 'application/json' });
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = filename;
  a.click();
  URL.revokeObjectURL(url);
}

export function importFromJson<T>(): Promise<T> {
  return new Promise((resolve, reject) => {
    const input = document.createElement('input');
    input.type = 'file';
    input.accept = '.json';
    input.onchange = (e) => {
      const file = (e.target as HTMLInputElement).files?.[0];
      if (!file) return reject(new Error('No file selected'));
      const reader = new FileReader();
      reader.onload = () => {
        try {
          resolve(JSON.parse(reader.result as string) as T);
        } catch {
          reject(new Error('Invalid JSON file'));
        }
      };
      reader.readAsText(file);
    };
    input.click();
  });
}
```

- [ ] **Step 6: 验证 TypeScript 编译**

```bash
npx tsc --noEmit
```

预期: 0 errors。

- [ ] **Step 7: 提交**

```bash
git add src/types/ src/utils/ src/components/ src/pages/ src/hooks/ src/services/ src/store/ src/templates/
git commit -m "feat: create directory structure, type definitions, and storage utils"
```

---

### Task 6: 创建基础 App Shell（路由 + 首页占位）

**Files:**
- Create: `src/pages/Home.tsx`
- Modify: `src/App.tsx`, `src/main.tsx`

- [ ] **Step 1: 创建占位首页**

创建 `src/pages/Home.tsx`:
```typescript
export default function Home() {
  return (
    <div className="min-h-screen bg-[var(--color-bg)] flex flex-col items-center justify-center">
      <h1 className="text-4xl font-bold text-[var(--color-primary-dark)] mb-4">
        Resume Builder
      </h1>
      <p className="text-gray-500 text-lg mb-8">
        AI 驱动的智能简历制作平台
      </p>
      <nav className="flex gap-4">
        <a href="/profile" className="px-6 py-3 bg-[var(--color-primary)] text-white rounded-lg hover:opacity-90 transition-opacity">
          开始制作
        </a>
        <a href="/settings" className="px-6 py-3 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors">
          设置
        </a>
      </nav>
    </div>
  );
}
```

- [ ] **Step 2: 修改 App.tsx — 添加路由**

修改 `src/App.tsx` 内容为：
```typescript
import { BrowserRouter, Routes, Route } from 'react-router-dom';
import Home from './pages/Home';

function PlaceholderPage({ title }: { title: string }) {
  return (
    <div className="min-h-screen bg-[var(--color-bg)] flex items-center justify-center">
      <div className="text-center">
        <h2 className="text-2xl font-bold text-[var(--color-primary-dark)] mb-2">{title}</h2>
        <p className="text-gray-400">开发中...</p>
        <a href="/" className="text-[var(--color-primary)] mt-4 inline-block hover:underline">← 返回首页</a>
      </div>
    </div>
  );
}

export default function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="/profile" element={<PlaceholderPage title="经历管理" />} />
        <Route path="/jd" element={<PlaceholderPage title="JD 匹配" />} />
        <Route path="/editor" element={<PlaceholderPage title="简历编辑" />} />
        <Route path="/settings" element={<PlaceholderPage title="设置" />} />
      </Routes>
    </BrowserRouter>
  );
}
```

- [ ] **Step 3: 保持 main.tsx 不变**

Vite 脚手架生成的 `src/main.tsx` 保持默认即可：
```typescript
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```

- [ ] **Step 4: 验证路由可用**

```bash
npm run dev
```

预期: 首页显示"Resume Builder"标题和两个按钮。手动访问 `/profile`、`/settings` 等路径显示占位页面。Ctrl+C 停止。

- [ ] **Step 5: 提交**

```bash
git add src/pages/Home.tsx src/App.tsx
git commit -m "feat: add app shell with routing and placeholder pages"
```

---

### Task 7: 配置 GitHub Actions 自动部署

**Files:**
- Create: `.github/workflows/deploy.yml`

- [ ] **Step 1: 创建部署 workflow**

创建 `.github/workflows/deploy.yml`:
```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

- [ ] **Step 2: 确认 vite.config.ts 的 base 路径**

`vite.config.ts` 中的 `base` 需要匹配 GitHub 仓库名。如果仓库名为 `resume-builder`，则 `base: '/resume-builder/'`。如果使用自定义域名或用户名.github.io 仓库，则 `base: '/'`。

```typescript
// 如果仓库是 https://github.com/<user>/resume-builder
base: '/resume-builder/',
```

- [ ] **Step 3: 提交**

```bash
git add .github/workflows/deploy.yml vite.config.ts
git commit -m "ci: configure GitHub Actions for GitHub Pages deployment"
```

---

### Task 8: 关联 GitHub 远程仓库并推送

**Files:**
- Create: `README.md`

- [ ] **Step 1: 创建 README.md**

````markdown
# Resume Builder

AI 驱动的智能简历制作平台。

## 技术栈

React + Vite + TypeScript + Tailwind CSS

## 本地开发

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
npm run preview
```
````

- [ ] **Step 2: 创建 GitHub 仓库并推送**

用户需要先在 GitHub 上创建仓库（如 `resume-builder`），然后：

```bash
cd D:/Claude/projects/简历制作
git remote add origin https://github.com/<your-username>/resume-builder.git
git branch -M main
git add README.md
git commit -m "docs: add README"
git push -u origin main
```

- [ ] **Step 3: 验证 GitHub Actions 部署**

推送后访问 GitHub 仓库的 Actions 页面，确认 workflow 运行成功。部署完成后访问 `https://<your-username>.github.io/resume-builder/` 确认页面可访问。

---

## 完成检查清单

- [ ] `npm run dev` 启动无错误
- [ ] `npm run build` 构建成功
- [ ] `npm run lint` 无 error
- [ ] `npx tsc --noEmit` 无类型错误
- [ ] 路由 `/`, `/profile`, `/jd`, `/editor`, `/settings` 均可访问
- [ ] Tailwind CSS 样式生效
- [ ] GitHub Actions 自动部署成功
- [ ] GitHub Pages URL 可访问
